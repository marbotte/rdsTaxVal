formatTaxonomy<-function(obj,currentFormat=c("oneTable","tableByPlot_list"),family="family", genus="genus", epithet="specificEpithet", infraspecies="infraspecificEpithet")
# Function to create the object that will be used to check for inconsistencies later
# 1. check whether it is one table, or 2
# 2. check spaces, indetermined, cf. and aff.
{
  
}

checkSpace <- function(obj,family="family", genus="genus", epithet="specificEpithet", infraspecies="infraspecificEpithet")
{
  
}

indetermitedEtc <- function(obj, family="family", genus="genus", epithet="specificEpithet", infraspecies="infraspecificEpithet")
# Note: when there are cases of Indet or Morpho, species code should be checked for relevant information
{
  
}  
  
unicity <- function(obj, family="family",  genus="genus", epithet="specificEpithet", infraspecies="infraspecificEpithet")
{
  
}



familtySearchGbif<-function(obj)
  

genusSearchGbif <- function(obj)
  

spSearchGbif <- function(obj)